{
  "name": "tourism-scheduler-miyagi",
  "version": "1.0.0",
  "scripts": {
    "build": "cd server && npm ci && npm run download-gtfs && npm run import-gtfs",
    "start": "cd server && npm start"
  },
  "dependencies": {
    "puppeteer": "^24.33.0"
  }
}
